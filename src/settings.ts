/** The name of the plugin package, as Homebridge knows it. */
export const PLUGIN_NAME = 'homebridge-vacuum-dreame';

/** The platform name users put in their Homebridge config. */
export const PLATFORM_NAME = 'DreameVacuum';
